---
title: Feature scaling
---

`q.preprocess.scale` is reserved for min-max, standard, robust, max-absolute, and
related numeric scaling.

The namespace is currently empty. Learned scaling parameters must be fitted on
training data only.