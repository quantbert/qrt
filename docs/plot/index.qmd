---
title: Plotting
---

`q.plot` renders interactive Plotly figures for feature relationships, labels,
multiclass model evaluation, return streams, and trade logs. `q.plot.correlation`
creates a linked scatterplot matrix with categorical regime or continuous-outcome
coloring. `q.plot.labels` overlays long,
hold, and short events on price, with optional outcome-interval shading; see
the [labeling examples](../label/index.qmd). Classification diagnostics include one-vs-rest
ROC and precision-recall curves with per-class, micro-average, and macro-average
summaries. Return and trade visualizations cover quick column charts, equity
curves, drawdowns, calendar heatmaps, full quantstats-style tearsheets, Monte
Carlo diagnostics, entry/exit markers, excursions, and per-trade distributions.
Return-stream *statistics* (performance, alpha/beta, rolling diagnostics,
`q.stats.montecarlo`, ...) live in `q.stats`; see
[Return Statistics](../stats/index.qmd). For market measurements, see
[Indicators](../indicator/index.ipynb).

Pages:

- [Feature relationships](correlation.ipynb) — linked scatterplot matrices colored
  by market regime, asset, split, or continuous forward outcome.
- [Classification curves](classification.ipynb) — multiclass ROC and
  precision-recall curves, plus reusable tidy curve data from `q.stats`.
- [Return charts](returns.ipynb) — column charts, equity curves, drawdowns,
  full performance reports, and monthly heatmaps.
- [Tearsheet report](report.ipynb) — full quantstats-style strategy tearsheet
  (`q.plot.report`) plus its individual panels (metrics table, cumulative
  returns, EOY returns, rolling diagnostics, worst drawdowns, ...).
- [Simulation tests](simulation.ipynb) — Monte Carlo fan charts and outcome
  distributions, variance testing, and noise testing.
- [Trade logs](trades.ipynb) — trade markers on price, MAE/MFE excursions,
  per-trade return distributions, and the trades → tearsheet bridge.
- [Saving figures](saving.qmd) — exporting any figure to HTML/PNG.
