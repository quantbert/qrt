---
title: Missing data
---

`q.transform.impute` is reserved for fitted missing-data handling, including
simple imputers, KNN, and MICE.

The namespace is currently empty. Fitted imputers must learn only from fit
partitions. Deterministic gap detection and data canonicalization belong in
`q.data`.